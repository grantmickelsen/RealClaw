# TEMPLATE ONLY — do NOT apply this file directly. Values are intentionally blank.
#
# Create the secret via CI pipeline before deploying:
#
#   kubectl create secret generic realclaw-secrets --namespace=realclaw \
#     --from-literal=JWT_SECRET=<32-byte-hex> \
#     --from-literal=DATABASE_URL=postgresql://user:pass@host:5432/realclaw \
#     --from-literal=REDIS_URL=redis://:pass@host:6379 \
#     --from-literal=CLAW_VAULT_MASTER_KEY=<32-byte-hex> \
#     --from-literal=CLAW_MANIFEST_API_KEY=<key> \
#     --from-literal=ANTHROPIC_API_KEY=<key> \
#     --from-literal=OPENAI_API_KEY=<key> \
#     --from-literal=APPLE_CLIENT_ID=<bundle-id> \
#     --from-literal=GOOGLE_CLIENT_ID=<client-id> \
#     --from-literal=POSTGRES_PASSWORD=<pass> \
#     --dry-run=client -o yaml | kubectl apply -f -
#
# NEVER commit actual secret values to this file or any other file in this repo.
apiVersion: v1
kind: Secret
metadata:
  name: realclaw-secrets
  namespace: realclaw
type: Opaque
stringData:
  JWT_SECRET: "<REPLACE>"
  DATABASE_URL: "<REPLACE>"
  REDIS_URL: "<REPLACE>"
  CLAW_VAULT_MASTER_KEY: "<REPLACE>"
  CLAW_MANIFEST_API_KEY: "<REPLACE>"
  ANTHROPIC_API_KEY: "<REPLACE>"
  OPENAI_API_KEY: "<REPLACE>"
  APPLE_CLIENT_ID: "<REPLACE>"
  GOOGLE_CLIENT_ID: "<REPLACE>"
  POSTGRES_PASSWORD: "<REPLACE>"
