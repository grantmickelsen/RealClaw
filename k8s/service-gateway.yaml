apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: realclaw
  labels:
    app: gateway
spec:
  selector:
    app: gateway
  ports:
  - name: http
    port: 80
    targetPort: 18789
  type: ClusterIP
